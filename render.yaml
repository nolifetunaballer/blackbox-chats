services:
- type: web
  name: blackbox-chat
  runtime: python
  buildCommand: pip install -r requirements.txt
  startCommand: gunicorn app:app
  healthCheckPath: /health
  envVars:
  - key: SECRET_KEY
    generateValue: true
